# 印刷行业综合管理系统需求文档

## 项目概述
构建一套面向印刷行业的综合管理系统，包含用户权限、订单管理、刀模管理、客户管理及数据统计五大模块。系统需提供完整的增删改查功能，并针对印刷业务流程进行特定优化。

---

## 1. 用户与权限模块

### 1.1 核心功能
- **登录/注册**：支持账号密码登录，首次使用可注册账号。
- **JWT认证**：采用JWT进行无状态认证，保障接口安全。
- **RBAC权限管理**：实现用户-角色-权限的灵活绑定。
- **菜单权限**：前端路由与菜单根据用户角色动态生成与显示。
- **角色管理**：支持角色的创建、编辑、删除及权限分配。
- **操作日志**：记录关键操作的用户、时间、行为及结果，便于审计。

### 1.2 功能需求清单
| 功能点 | 说明 |
|--------|------|
| 登录 | 用户名+密码登录，返回JWT Token |
| 注册 | 填写基本信息完成注册，默认分配普通员工角色 |
| 用户管理 | 增删改查，分配角色 |
| 角色管理 | 增删改查，配置权限 |
| 权限分配 | 为角色勾选可访问的菜单和操作按钮 |
| 菜单管理 | 配置系统菜单结构，支持多级 |
| 操作日志 | 记录操作人、操作内容、时间、IP，支持查询和导出 |

### 1.3 预设角色
| 角色 | 权限范围 | 说明 |
|------|----------|------|
| 超级管理员 | 全部权限 | 系统配置、用户管理、所有业务模块操作与数据查看 |
| 财务人员 | 订单查看、数据统计 | 查看订单及金额，操作统计报表，无刀模/客户管理权限 |
| 普通员工 | 订单、刀模、客户 | 负责日常录单、刀模位置打印、客户信息维护，无系统管理权限 |

---

## 2. 订单管理系统

### 2.1 功能需求
- **基础操作**：订单的增、删、改、查。
- **Excel导入导出**：支持按指定字段模板进行批量导入与导出。
- **送货单打印**：勾选订单后生成并打印送货单。

### 2.2 数据字段
| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 下单日期 | 日期 | 是 | 订单创建日期 |
| 送货单号 | 字符串 | 是 | 送货单编号 |
| 印刷名称 | 字符串 | 是 | 印刷品名称 |
| 数量 | 整数 | 是 | 印刷数量 |
| 单价 | 浮点数 | 是 | 产品单价 |
| 金额 | 浮点数 | 是 | 总金额（数量×单价自动计算） |
| 排单号 | 字符串 | 否 | 生产排单编号 |
| 订单号 | 字符串 | 是 | 系统自动生成唯一订单号 |
| 客户名称 | 关联字段 | 是 | 关联客户管理模块，支持选择和快速新增 |
| 刀模型号 | 关联字段 | 否 | 关联刀模管理模块，支持选择和快速新增 |
| 不干胶材料 | 字符串 | 否 | 所用材料名称或编号 |
| 备注 | 文本 | 否 | 附加说明 |
| 是否出货 | 布尔 | 是 | 出货状态标识，默认未出货 |
| 送货日期 | 日期 | 否 | 实际送货日期 |
| 其他 | 字符串 | 否 | 扩展字段 |
| 冗余字段 | 字符串 | 否 | 备用扩展 |

### 2.3 业务规则
1. **快速新增**：新增订单时，若客户名称或刀模型号在系统中不存在，输入框旁显示"新增"按钮，点击弹出新增窗口，填写后自动回填到订单表单。
2. **送货单打印规则**：
   - 勾选多条订单后可生成送货单。
   - 仅允许勾选同一客户的订单；若勾选了不同客户的订单，系统提示"请仅勾选同一客户的订单"。
   - 送货单需区分客户，不同客户生成独立送货单。

---

## 3. 刀模管理系统

### 3.1 功能需求
- **基础操作**：刀模的增、删、改、查。
- **型号标识**：根据刀模形状自动或手动生成型号。
- **位置标记**：采用结构化编码标记刀模仓储位置。
- **标签打印**：支持打印刀模位置标签，支持批量打印。

### 3.2 型号生成规则
| 形状类型 | 型号规则 | 示例 |
|----------|----------|------|
| 矩形/正方形 | 长×宽 | 50×10 |
| 圆形 | 直径D | D100 |
| 异型刀模 | 异型-{自定义名称或序号} | 异型-001 或 异型-特殊弧形 |

### 3.3 位置编码方案
采用**区域-排-序号**三级定位法。
- **编码格式**：`{区域代码}-{排号}-{序号}`
- **示例**：
  - `A-5-1` 表示 A区 第5排 第1个位置
  - `B-3-12` 表示 B区 第3排 第12个位置
  - `C-1-8` 表示 C区 第1排 第8个位置
- **扩展**：如需增加层位，可扩展为 `{区域}-{排}-{序号}-{层}`，如 `A-5-1-3`

### 3.4 数据字段
| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 刀模编号 | 字符串 | 是 | 系统自动生成 |
| 刀模名称 | 字符串 | 是 | 自定义名称 |
| 形状类型 | 枚举 | 是 | 矩形/圆形/异型 |
| 长度 | 浮点数 | 矩形必填 | 单位mm |
| 宽度 | 浮点数 | 矩形必填 | 单位mm |
| 直径 | 浮点数 | 圆形必填 | 单位mm |
| 型号 | 字符串 | 是 | 根据规则自动生成，可手动修改 |
| 区域 | 字符串 | 是 | 存放区域，如A、B、C |
| 排号 | 整数 | 是 | 第几排 |
| 序号 | 整数 | 是 | 第几个位置 |
| 位置编码 | 字符串 | 是 | 自动生成，如A-5-1 |
| 备注 | 文本 | 否 | 附加说明 |
| 状态 | 枚举 | 是 | 在库/已出库 |

### 3.5 打印需求
- **打印内容**：位置编码、刀模型号、尺寸、备注。
- **打印方式**：支持单条打印和批量勾选打印。
- **标签规格**：标准标签尺寸，适配常见标签打印机。

---

## 4. 客户管理系统

### 4.1 功能需求
- **基础操作**：客户信息的增、删、改、查。
- **Excel导入导出**：支持客户数据的批量导入与导出。

### 4.2 数据字段
| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 客户编号 | 字符串 | 是 | 系统自动生成 |
| 客户名称 | 字符串 | 是 | 公司或个人名称 |
| 联系人 | 字符串 | 否 | 主要联系人 |
| 联系电话 | 字符串 | 是 | 手机号或座机 |
| 地址 | 字符串 | 否 | 公司地址 |
| 邮箱 | 字符串 | 否 | 电子邮箱 |
| 备注 | 文本 | 否 | 附加说明 |
| 创建时间 | 日期 | 是 | 自动生成 |

---

## 5. 数据统计系统

### 5.1 订单统计
- 订单总量统计（按日/月/年）
- 销售额趋势图
- 出货率统计（已出货/未出货比例）
- 各客户订单量排行
- 各客户销售额排行

### 5.2 刀模统计
- 刀模总量统计
- 各型号刀模数量分布
- 刀模使用频率统计
- 各区域位置占用情况

### 5.3 客户统计
- 客户总量统计
- 新客户增长趋势（按月）
- 客户活跃度排行（按订单数）
- 客户贡献度排行（按订单金额）

### 5.4 展示方式
- 以Dashboard仪表盘形式可视化展示。
- 支持按时间范围筛选。
- 关键指标卡片式展示，趋势使用折线图，占比使用饼图，排行使用柱状图。

---

## 6. 技术选型

| 层级 | 技术 | 版本 |
|------|------|------|
| 前端框架 | Vue 3 | 最新稳定版 |
| UI组件库 | Element Plus | 最新稳定版 |
| 状态管理 | Pinia | 最新稳定版 |
| 构建工具 | Vite | 最新稳定版 |
| 后端框架 | Java Spring Boot | 3.x |
| 安全框架 | Spring Security | 6.x |
| ORM框架 | MyBatis-Plus | 3.x |
| 项目管理 | Maven | 3.x |
| 数据库 | MySQL | 8.0 |
| 缓存 | Redis | 7.x |
| 认证方式 | JWT | - |

---

## 7. UI风格

采用**苹果风**设计风格，具体要求：
- 大面积留白，布局简洁
- 圆角卡片式容器
- 轻盈的弥散阴影
- 清爽的配色方案（浅色背景为主）
- 字体清晰，层级分明
- 图标简洁统一

---

## 8. 数据库核心表结构

### 8.1 用户权限相关
- `sys_user` - 用户表
- `sys_role` - 角色表
- `sys_permission` - 权限表
- `sys_user_role` - 用户角色关联表
- `sys_role_permission` - 角色权限关联表
- `sys_menu` - 菜单表
- `sys_operation_log` - 操作日志表

### 8.2 业务相关
- `t_order` - 订单表
- `t_customer` - 客户表
- `t_knife_mold` - 刀模表

---

## 9. 开发计划

| 阶段 | 内容 | 预计产出 |
|------|------|----------|
| 第一阶段 | 项目搭建、数据库设计、用户权限模块 | 登录注册、角色权限可用 |
| 第二阶段 | 客户管理、刀模管理 | 客户和刀模CRUD完成 |
| 第三阶段 | 订单管理 | 订单CRUD、Excel导入导出、送货单打印 |
| 第四阶段 | 数据统计 | Dashboard仪表盘、各类统计图表 |
| 第五阶段 | 联调测试、UI优化 | 前后端联调、苹果风UI定稿 |
| 第六阶段 | 部署上线 | 生产环境部署 |
这份文档已去除所有推荐对比内容，保留了完整的功能需求、字段定义、业务规则和技术选型，可以直接作为开发参考使用。

本回答由 AI 生成，内容仅供参考，请仔细甄别。
再根据这份文档 ,综合优化下,生成一份直接输出的,比如开发规范那些我觉得可以借鉴
综合两份文档的优点，我为你生成了一份兼顾业务细节和开发规范的完整技术需求文档，直接可用。

markdown
# 印刷行业管理系统 - 完整技术需求文档

> **文档用途**：可直接投喂AI进行开发，或作为团队开发参考规范
> **适用对象**：全栈开发工程师、AI编程助手

---

## 角色设定

你现在是一名资深全栈架构师 + 产品经理 + UI设计师 + Java高级开发工程师。请严格按照本文档需求，按模块逐步完成系统开发。

---

## 一、项目概述

### 1.1 项目名称
印刷行业综合管理系统

### 1.2 项目目标
开发一套适用于印刷行业的后台管理系统，覆盖订单管理、客户管理、刀模管理和数据统计四大核心业务，支撑企业内部协同办公。

### 1.3 核心要求
- 所有模块支持增删改查
- Excel导入导出
- 标签打印与送货单打印
- 预留ERP扩展能力
- 预留PDA扫码接口
- 前后端完全分离
- 模块化、低耦合、高扩展

---

## 二、技术栈

### 2.1 前端
| 技术 | 说明 |
|------|------|
| Vue 3 | 前端框架 |
| TypeScript | 类型安全 |
| Vite | 构建工具 |
| Pinia | 状态管理 |
| Vue Router | 路由管理 |
| Element Plus | UI组件库 |
| ECharts | 数据可视化 |
| Axios | HTTP请求 |

### 2.2 后端
| 技术 | 说明 |
|------|------|
| Java 17+ | 运行环境 |
| Spring Boot 3.x | 后端框架 |
| Spring Security 6.x | 安全框架 |
| JWT | 无状态认证 |
| MyBatis Plus | ORM框架 |
| Maven | 项目管理 |
| Knife4j | 接口文档 |
| Redis | 缓存 |

### 2.3 数据库
| 技术 | 版本 |
|------|------|
| MySQL | 8.0 |
| Redis | 7.x |

---

## 三、UI设计规范

### 3.1 设计风格：苹果风
- 大面积留白，布局极简
- 圆角卡片式容器
- 轻盈弥散阴影（box-shadow透明度不超过0.08）
- 浅色背景为主，支持暗黑模式切换
- 字体：系统默认中文字体，层级分明
- 图标：简洁线性图标
- 动效：过渡自然，使用ease-in-out曲线
- 支持标签页模式（多页面同时打开）

### 3.2 页面布局
- 左侧可折叠菜单栏（宽度220px）
- 顶部面包屑导航 + 用户信息
- 主内容区使用卡片包裹
- 列表页标准模式：搜索栏 + 操作按钮 + 表格 + 分页

---

## 四、系统角色与权限

### 4.1 预设角色
| 角色 | 权限范围 |
|------|----------|
| 超级管理员 | 全部权限（含系统配置、用户管理） |
| 财务人员 | 订单查看、数据统计 |
| 普通员工 | 订单录入、客户管理、刀模管理 |

### 4.2 RBAC权限模型
- 用户 → 角色 → 权限 三层绑定
- 菜单权限：控制页面访问
- 按钮权限：控制操作按钮显示
- 接口权限：后端接口级别控制

---

## 五、系统模块划分
印刷行业管理系统
├── 用户与权限模块
│ ├── 登录/注册/修改密码
│ ├── 用户管理（增删改查）
│ ├── 角色管理（增删改查+分配权限）
│ ├── 菜单管理（树形配置）
│ └── 操作日志（查询+导出）
├── 订单管理模块
│ ├── 订单增删改查
│ ├── 多条件搜索+分页
│ ├── Excel导入导出
│ ├── 送货单打印（批量+客户隔离）
│ └── 智能新增（自动关联客户和刀模）
├── 客户管理模块
│ ├── 客户增删改查
│ └── Excel导入导出
├── 刀模管理模块
│ ├── 刀模增删改查
│ ├── 型号自动生成
│ ├── 位置编码管理
│ └── 标签打印（批量+二维码）
└── 数据统计模块
├── 订单统计（Dashboard）
├── 刀模统计
├── 客户统计
└── ECharts可视化

text

---

## 六、开发规范（重要）

### 6.1 后端规范
RESTful API设计，URL使用小写+下划线

统一返回格式：{ code, msg, data, timestamp }

全局异常处理，区分业务异常和系统异常

接口权限控制：使用@PreAuthorize注解

操作日志：使用自定义注解@Log + AOP切面

分页请求统一封装，分页响应统一格式

代码分层：controller → service → mapper

不允许在controller写业务逻辑

DTO/VO分离，禁止直接返回实体类

SQL关键词大写，字段名小写+下划线

text

### 6.2 前端规范
组件化开发，页面=布局组件+业务组件

统一请求封装（Axios拦截器+Token自动携带）

统一错误处理（网络错误、业务错误提示）

表单统一校验规则

表格操作列统一使用el-dropdown

删除操作必须有二次确认

页面加载状态统一处理（loading骨架屏）

text

### 6.3 数据库规范
表名：sys_前缀（系统表），t_前缀（业务表）

字段名：小写+下划线

每表必有：id, create_time, update_time, create_by, update_by

索引命名：idx_表名_字段名

所有字段必须有注释

软删除使用is_deleted字段，禁止物理删除

text

### 6.4 统一返回格式
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {},
  "timestamp": 1715241600000
}
状态码规范：

code	含义
200	成功
400	参数错误
401	未授权
403	无权限
404	资源不存在
500	服务器错误
七、数据库设计
7.1 ER图（文字描述）
text
sys_user ──┐
            ├── sys_user_role ── sys_role ── sys_role_menu ── sys_menu
            │                   └── sys_role_permission
sys_operation_log

t_order ───────────────────────── t_customer
   │
   └────────────────────── t_knife_mold
7.2 完整建表SQL
请根据以下表结构生成完整可执行的SQL文件：

系统表
sys_user 用户表

字段	类型	说明
id	bigint	主键自增
username	varchar(50)	用户名，唯一
password	varchar(200)	加密密码
real_name	varchar(50)	真实姓名
phone	varchar(20)	手机号
email	varchar(100)	邮箱
status	tinyint	0禁用 1启用
create_time	datetime	创建时间
update_time	datetime	更新时间
create_by	bigint	创建人ID
update_by	bigint	更新人ID
is_deleted	tinyint	0正常 1删除
sys_role 角色表

字段	类型	说明
id	bigint	主键自增
role_name	varchar(50)	角色名称
role_code	varchar(50)	角色编码
description	varchar(200)	描述
status	tinyint	状态
create_time	datetime	创建时间
update_time	datetime	更新时间
is_deleted	tinyint	软删除
sys_menu 菜单表

字段	类型	说明
id	bigint	主键自增
parent_id	bigint	父菜单ID
menu_name	varchar(50)	菜单名称
path	varchar(200)	路由路径
component	varchar(200)	组件路径
icon	varchar(50)	图标
sort	int	排序
menu_type	tinyint	0目录 1菜单 2按钮
permission	varchar(100)	权限标识
status	tinyint	状态
create_time	datetime	创建时间
update_time	datetime	更新时间
is_deleted	tinyint	软删除
sys_user_role 用户角色关联表

字段	类型	说明
id	bigint	主键
user_id	bigint	用户ID
role_id	bigint	角色ID
sys_role_menu 角色菜单关联表

字段	类型	说明
id	bigint	主键
role_id	bigint	角色ID
menu_id	bigint	菜单ID
sys_operation_log 操作日志表

字段	类型	说明
id	bigint	主键
user_id	bigint	操作人ID
username	varchar(50)	操作人用户名
operation	varchar(100)	操作描述
method	varchar(200)	请求方法
params	text	请求参数
ip	varchar(50)	IP地址
status	tinyint	0失败 1成功
error_msg	text	错误信息
cost_time	bigint	耗时(ms)
create_time	datetime	操作时间
业务表
t_customer 客户表

字段	类型	必填	说明
id	bigint	是	主键自增
customer_no	varchar(50)	是	客户编号，系统生成
customer_name	varchar(100)	是	客户名称
contact_person	varchar(50)	否	联系人
phone	varchar(20)	是	联系电话
address	varchar(200)	否	地址
email	varchar(100)	否	邮箱
customer_level	varchar(20)	否	客户等级：A/B/C
remark	text	否	备注
create_time	datetime	是	创建时间
update_time	datetime	是	更新时间
create_by	bigint	否	创建人
update_by	bigint	否	更新人
is_deleted	tinyint	是	软删除
t_knife_mold 刀模表

字段	类型	必填	说明
id	bigint	是	主键自增
mold_no	varchar(50)	是	刀模编号，系统生成
mold_name	varchar(100)	是	刀模名称
shape_type	varchar(20)	是	形状：RECTANGLE/SQUARE/CIRCLE/OVAL/CUSTOM
length	decimal(10,2)	否	长度mm（矩形必填）
width	decimal(10,2)	否	宽度mm（矩形必填）
diameter	decimal(10,2)	否	直径mm（圆形必填）
model	varchar(50)	是	型号（自动生成）
area_code	varchar(10)	是	区域：A/B/C
shelf_no	varchar(10)	是	货架号
layer_no	varchar(10)	是	层号
position_no	varchar(10)	是	位置号
location_code	varchar(50)	是	完整位置编码：A-05-02-01
status	varchar(20)	是	IN_STOCK/OUT_STOCK
remark	text	否	备注
create_time	datetime	是	创建时间
update_time	datetime	是	更新时间
create_by	bigint	否	创建人
update_by	bigint	否	更新人
is_deleted	tinyint	是	软删除
t_order 订单表

字段	类型	必填	说明
id	bigint	是	主键自增
order_no	varchar(50)	是	订单号，系统生成
order_date	datetime	是	下单日期
delivery_no	varchar(50)	是	送货单号
print_name	varchar(200)	是	印刷名称
quantity	int	是	数量
unit_price	decimal(10,2)	是	单价
amount	decimal(10,2)	是	金额（自动计算）
schedule_no	varchar(50)	否	排单号
material	varchar(100)	否	不干胶材料
customer_id	bigint	是	客户ID（外键）
customer_name	varchar(100)	是	客户名称（冗余）
mold_id	bigint	否	刀模ID（外键）
mold_name	varchar(100)	否	刀模名称（冗余）
remark	text	否	备注
shipped	tinyint	是	是否出货：0否 1是
delivery_date	datetime	否	送货日期
extra_info	varchar(200)	否	扩展字段
reserve_field	varchar(200)	否	冗余字段
create_time	datetime	是	创建时间
update_time	datetime	是	更新时间
create_by	bigint	否	创建人
update_by	bigint	否	更新人
is_deleted	tinyint	是	软删除
7.3 索引设计
sql
-- 订单表索引
CREATE INDEX idx_order_order_no ON t_order(order_no);
CREATE INDEX idx_order_customer_id ON t_order(customer_id);
CREATE INDEX idx_order_order_date ON t_order(order_date);
CREATE INDEX idx_order_shipped ON t_order(shipped);

-- 刀模表索引
CREATE INDEX idx_mold_location_code ON t_knife_mold(location_code);
CREATE INDEX idx_mold_status ON t_knife_mold(status);

-- 客户表索引
CREATE INDEX idx_customer_name ON t_customer(customer_name);

-- 用户表索引
CREATE UNIQUE INDEX idx_user_username ON sys_user(username);
八、接口设计规范
8.1 RESTful URL设计
text
GET    /api/orders              查询订单列表（分页）
GET    /api/orders/{id}         查询订单详情
POST   /api/orders              新增订单
PUT    /api/orders/{id}         更新订单
DELETE /api/orders/{id}         删除订单
POST   /api/orders/export       导出订单Excel
POST   /api/orders/import       导入订单Excel
POST   /api/orders/print        批量打印送货单
8.2 分页请求格式
json
{
  "pageNum": 1,
  "pageSize": 20,
  "keyword": "搜索关键词",
  "startDate": "2026-01-01",
  "endDate": "2026-05-09",
  "shipped": 0
}
8.3 分页响应格式
json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "records": [],
    "total": 100,
    "pageNum": 1,
    "pageSize": 20,
    "pages": 5
  },
  "timestamp": 1715241600000
}
九、核心业务逻辑
9.1 订单-智能新增逻辑
text
1. 用户填写订单表单
2. 输入客户名称时：
   - 前端实时搜索现有客户列表
   - 若匹配到：选择后自动填充客户ID
   - 若未匹配到：输入框旁显示"新增客户"按钮
3. 点击"新增客户"：
   - 弹出客户新增对话框
   - 填写客户信息并保存
   - 自动回填到订单表单
4. 刀模选择逻辑同上
5. 订单保存时校验客户ID和刀模ID的有效性
9.2 送货单-打印隔离规则
text
1. 用户勾选多条订单记录
2. 点击"打印送货单"
3. 系统后端校验：
   - 提取所有勾选订单的customer_id
   - 若customer_id去重后数量 > 1：
     返回错误：{ code: 400, msg: "请仅勾选同一客户的订单进行打印" }
   - 若全部为同一客户：
     生成该客户的送货单PDF
4. 返回PDF文件流供前端打印
9.3 刀模-型号自动生成规则
text
若 shape_type = 'RECTANGLE' → model = length + "×" + width      （如 50×10）
若 shape_type = 'SQUARE'    → model = length + "×" + width      （如 20×20）
若 shape_type = 'CIRCLE'    → model = "Φ" + diameter            （如 Φ50）
若 shape_type = 'OVAL'      → model = "OV-" + length + "×" + width
若 shape_type = 'CUSTOM'    → model = "CUSTOM-" + 用户自定义编号
9.4 刀模-位置编码生成规则
text
location_code = area_code + "-" + 
                LPAD(shelf_no, 2, '0') + "-" + 
                LPAD(layer_no, 2, '0') + "-" + 
                LPAD(position_no, 2, '0')

示例：A-05-02-01
含义：A区域 - 第05号货架 - 第02层 - 第01个位置
十、打印功能设计
10.1 送货单打印
生成A4尺寸PDF

包含：客户名称、送货单号、产品明细表格、日期

支持批量勾选生成（需同一客户）

前端调用PDF文件流进行打印

10.2 刀模标签打印
标签尺寸：50mm×30mm（适配热敏标签打印机）

包含内容：

刀模编号（如 DM-0001）

型号（如 50×10）

位置编码（如 A-05-02-01）

二维码（编码刀模ID，供PDA扫描）

创建日期

支持单个打印和批量勾选打印

十一、前端页面清单
页面	路由	组件名
登录页	/login	LoginView
首页仪表盘	/dashboard	DashboardView
订单管理	/order	OrderList
订单详情	/order/:id	OrderDetail
客户管理	/customer	CustomerList
刀模管理	/knife-mold	KnifeMoldList
数据统计	/statistics	StatisticsView
用户管理	/system/user	UserList
角色管理	/system/role	RoleList
菜单管理	/system/menu	MenuList
操作日志	/system/log	LogList
个人设置	/profile	ProfileView
十二、代码输出顺序
请严格按照以下顺序输出代码，每完成一步需确认后再进行下一步：

text
步骤1：输出系统整体架构说明
步骤2：输出完整SQL建表脚本（含索引和初始数据）
步骤3：输出后端项目结构和pom.xml
步骤4：输出配置文件（application.yml）
步骤5：输出公共模块（统一返回、异常处理、工具类）
步骤6：输出用户与权限模块（Spring Security + JWT + RBAC）
步骤7：输出客户管理模块（后端controller/service/mapper）
步骤8：输出刀模管理模块（含位置编码生成逻辑）
步骤9：输出订单管理模块（含打印和Excel功能）
步骤10：输出数据统计模块（含ECharts数据接口）
步骤11：输出前端项目结构
步骤12：输出前端登录和权限控制
步骤13：输出前端业务页面（客户/刀模/订单/统计）
步骤14：输出打印功能实现（送货单PDF + 标签打印）
步骤15：输出部署配置（Docker Compose + Nginx）
十三、输出要求（必读）
text
1. 所有代码必须完整可运行，禁止省略
2. 每个方法必须有注释
3. 每步完成需说明文件路径和用途
4. 数据库变更需提供SQL脚本
5. 接口变更需同步更新Knife4j文档
6. 前端组件需提供完整template+script+style
7. 复杂业务逻辑需提供流程图文字描述
8. 打印功能需提供完整实现方案
文档版本：v2.0
最后更新：2026-05-09
下一阶段：开始系统架构设计